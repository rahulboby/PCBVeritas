# ============================================================
# PCBVeritas requirements
# Single dependency file for local development and Streamlit Community Cloud.
# These defaults are CPU-friendly so non-LLM components do not consume GPU VRAM.
# ============================================================

# --- Packaging bootstrap ---
# setuptools>=68.2.2
setuptools<82.0.0
wheel>=0.42.0

# --- Core scientific stack ---
numpy
pandas
scipy==1.13.0
scikit-learn

# --- Image processing ---
Pillow==10.3.0
matplotlib==3.8.4
seaborn==0.13.2

# --- Deep learning ---
# Default install is CPU-friendly and works for Streamlit Cloud.
# For a local RTX 4050, install CUDA-enabled PyTorch manually after creating
# your venv, for example:
#   python -m pip install torch==2.2.2 torchvision==0.17.2 torchaudio==2.2.2 --index-url https://download.pytorch.org/whl/cu118
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2

# --- Object detection / XAI ---
ultralytics==8.2.18
grad-cam==1.5.0
# Keep OpenCV headless active for Streamlit/Cloud deployments.
# Install this after grad-cam/Ultralytics so it becomes the active cv2 runtime.
opencv-python-headless

# --- Vision-language embeddings ---
transformers==4.40.2

# --- LLM API ---
openai>=1.40.0

# --- Vector search ---
faiss-cpu

# --- Streamlit app ---
streamlit
streamlit-image-coordinates==0.1.6
plotly==5.22.0

# --- Utilities ---
PyYAML==6.0.1
tqdm==4.66.4
loguru==0.7.2
rich==13.7.1
click==8.1.7
python-dotenv==1.0.1

# --- Experiment tracking ---
mlflow==2.13.0

# --- Testing ---
pytest==8.2.0
pytest-cov==5.0.0
